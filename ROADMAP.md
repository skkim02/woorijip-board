# 로드맵 — "우리집 보드"를 범용 서비스로

부부(와이프·남편) 전용으로 시작한 앱을 **누구나 자기 그룹을 만들어 쓰는 범용 PWA 서비스**로
확장하기 위한 단계별 계획입니다. 앱스토어 등록 대신 **웹사이트에서 바로 설치(PWA)** 하는 방향을 택했습니다.

---

## 핵심 방향

- **설치 방식**: 앱스토어(유료) 대신 **PWA** — 웹에서 "홈 화면에 추가/앱 설치" (무료, 즉시 배포)
- **인증 방식**: 무거운 로그인 대신 **그룹 코드(6자리)** 방식 — 가볍게 만들고 참여 (앱의 성격 유지)
- **데이터 격리**: 모든 데이터는 `group_code` 로 그룹별 분리
- **하위 호환**: 기존 2인용 데이터는 `default` 그룹으로 그대로 유지 (마이그레이션해도 안 깨짐)

---

## Phase 1 — 범용 기반 + 설치 경험 ✅ (이번 작업)

- [x] **그룹(방) 분리**: `groups` 테이블 + `requests`/`push_subscriptions` 에 `group_code`
- [x] **온보딩**: 첫 방문 시 "그룹 만들기 / 코드로 참여" 화면
- [x] **멤버 자유화**: 와이프/남편 고정 → 직접 이름 입력, 인원수 자유
- [x] **초대**: 그룹 코드 공유 + 초대 링크(`?join=코드`)
- [x] **앱 설치 버튼**: 안드로이드/PC `beforeinstallprompt`, iOS 설치 안내 시트
- [x] **manifest 보강**: id/categories/display_override 등
- [x] **그룹 범위 푸시**: Edge Function 이 같은 그룹에게만 발송
- [x] **기존 사용자 자동 이전**: 예전 `whoami` 사용자는 `default` 그룹으로 연결

> 배포 전 1회: `supabase/migration-groups.sql` 을 SQL Editor 에서 실행해야 합니다.

---

## Phase 2 — 신뢰성/보안 강화

- [ ] **진짜 인증(선택)**: Supabase Auth (카카오/애플/구글) 도입
- [ ] **RLS 강화**: 현재는 anon 이 모든 행 접근 가능(클라이언트가 코드로 필터). 인증 도입 후
      "자기 그룹만" 접근하도록 행 수준 보안(RLS)을 그룹 멤버십 기준으로 제한
- [ ] **그룹 관리**: 멤버 내보내기/이름 변경/그룹 삭제, 코드 재발급
- [ ] **개인정보처리방침·이용약관** 페이지 (서비스 공개 시 필요)
- [ ] **악용 방지**: 코드 추측 방지(길이/레이트리밋), 그룹당 최대 인원

> ⚠️ 현재 보안 수준: 코드 방식은 "코드를 아는 사람만 들어온다" 수준입니다.
> 민감 데이터를 다루는 공개 서비스로 키울 경우 Phase 2 의 RLS/인증이 필요합니다.

---

## Phase 3 — 제품 완성도

- [ ] **랜딩 페이지**: 서비스 소개 + 설치 안내 (마케팅/온보딩 분리)
- [ ] **프리셋 커스터마이즈**: 자주 쓰는 요청 버튼을 그룹이 직접 편집
- [ ] **반복 요청**: 매주 분리수거 등 자동 등록
- [ ] **완료 기록/통계**: 누가 얼마나 처리했는지 (게이미피케이션 옵션)
- [ ] **다국어(i18n)**: 한국어 외 확장

---

## Phase 4 — (선택) 네이티브 앱

PWA 로 한계가 느껴질 때만:
- [ ] **Capacitor** 로 웹앱을 앱스토어용으로 래핑 (iOS/Android)
- [ ] Apple Developer($99/년), Google Play($25/1회) 등록

---

## 배포 체크리스트 (Phase 1)

1. `supabase/migration-groups.sql` 을 Supabase SQL Editor 에서 실행
2. Realtime: Database → Replication 에서 `requests` 활성 확인
3. Edge Function `notify-request` 재배포 (그룹 범위 발송 반영)
4. `main` 에 머지 → Vercel 자동 배포
5. 새 기기에서 접속 → 그룹 만들기/참여/설치/푸시 동작 확인
